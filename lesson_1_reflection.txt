How did viewing a diff between two versions of a file help you see the bug that
was introduced?

  Comparison made it easier to debug compared to eyeballing

How could having easy access to the entire history of a file make you a more efficient programmer in the long term? Allows for easier back tracking 

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does? Logical Commits but may be forgetful

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately? Git is for programming which requires multiple files

How can you use the commands git log and git diff to view the history of files? Use git Log to see commit history and git diff to see difference between commits